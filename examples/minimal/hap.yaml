hap: "0.2"
agent:
  name: hello-world
  version: 1.0.0
  description: Returns a greeting through process JSONL.
interfaces:
  - id: local
    type: process
    protocol: jsonl
    command: ["./hello-world", "serve"]
